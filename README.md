# ML prototype playground

These are a collection of prototypes used for testing different features from the *scikit learn* library, aiming towards the develpoment of an optimised Random Forest Classification Model, trained on the publicly avaliable *scikit learn breast cancer dataset.*
